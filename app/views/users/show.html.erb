<div class="contents row">
  <p><%= @name %>さんの投稿一覧</p>
  <p>材料や作り方が知りたい場合は画像右上のボタンから詳細ページへ</p>
  <% @recipes.each do |recipe| %>
    <div class="content_post" >
    <div class="more">
        <span><%= image_tag 'arrow_top.png' %></span>
        <ul class="more_list">
        <li>
            <%= link_to '詳細', recipe_path(recipe.id), method: :get %>
          </li>
        </ul>
      </div>
    <%= image_tag recipe.image, class: 'recipe-image' %>
      <p><%= recipe.title %></p>
    </div>
  <% end %>
</div>